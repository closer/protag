box: wercker/ruby
build:
  steps:
    - bundle-install
    - npm-install
    - plasticine/bower-install
    - script:
        name: build
        code: bundle exec middleman build
deploy:
  steps:
    - script:
        name: S3 Sync
        code: bundle exec middleman s3_sync --force
